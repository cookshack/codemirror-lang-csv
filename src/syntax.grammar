@top Program { expression* }

@skip { newline }

expression {
  String |
  Separator |
  Value
}

@tokens {
  Separator { $[,;] }

  Value { ![,;\r\n]+ }

  String { '"' (!["\\] | "\\" _)* '"' }

  newline { '\r'? '\n' }

  @precedence { String, Separator, Value }
}

@detectDelim
